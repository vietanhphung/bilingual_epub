import { Command } from "commander";

/**
 * Placeholder commands for functionality landing in later milestones.
 * Currently empty — kept as the landing point for anything still pending.
 */
export function registerStubCommands(_program: Command): void {
  // No stub commands remaining.
}
